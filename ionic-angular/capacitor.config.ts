import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionicangular',
  appName: 'ionic-angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
